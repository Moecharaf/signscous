[build]
builder = "NIXPACKS"
buildCommand = "npm --prefix backend/server ci"

[deploy]
startCommand = "npm --prefix backend/server run start"
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
